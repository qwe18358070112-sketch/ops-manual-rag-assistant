# 政企运维知识库问答助手测试报告

## 1. 测试目标

本次测试用于完成工作计划中的“项目测试”部分，验证系统是否已经具备：

- 可稳定运行
- 可重复演示
- 核心链路可回归验证

## 2. 测试范围

本次测试覆盖：

- 健康检查接口
- 文档目录和过滤选项接口
- 手册问答检索
- 历史案例检索
- 案例时间轴
- 专题总览
- 专题详情
- 案例详情
- 问答页中的案例引用反向跳转

## 3. 测试方式

测试方式：

- `unittest`
- `FastAPI TestClient`

测试文件：

- [test_api_regression.py](/home/lenovo/projects/ops-manual-rag-assistant/tests/test_api_regression.py)

执行命令：

```bash
cd /home/lenovo/projects/ops-manual-rag-assistant
.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v
```

## 4. 关键测试点

### 4.1 手册问答

- `视频平台资源共享怎么操作`

验收点：

- 返回结果数大于 0
- 结果来自 `manual`
- 有引用来源

### 4.2 历史案例检索

- `视频平台客户端登录不上怎么处理`
- `10.25.7.158 漏洞怎么跟进`

验收点：

- 返回结果数大于 0
- 结果来自 `case`
- 引用里带有 `detail_url`

### 4.3 时间轴 / 专题 / 详情

验收点：

- `case-timeline` 能按标签和日期返回结果
- `topic-view` 能返回专题
- `topic-detail` 能返回完整案例集合
- `case-detail` 能返回单条案例详情

### 4.4 引用反向跳转

验收点：

- `/search` 返回的案例引用可回到案例详情
- `/search-page` 页面 HTML 中包含案例详情链接

## 5. 当前结论

当前项目已经具备：

- 可重复执行的接口回归测试
- 覆盖核心演示链路的自动测试能力
- 满足当前工作计划中“项目测试”的基本要求

这意味着项目已经从“手工 smoke test”进入“有最小回归保障”的状态。
