# 本机联调记录

联调时间：2026-04-19

## 1. 自动化测试

执行：

```bash
cd /home/lenovo/projects/ops-manual-rag-assistant
.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v
```

结果：`13/13` 通过。

## 2. 验收脚本

执行：

```bash
.venv/bin/python scripts/run_acceptance_checks.py
```

结果：通过，报告写入 `docs/落地验收报告.md`。

## 3. 本机 HTTP 联调

启动服务：

```bash
.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8001
```

### 上传链路

在禁用代理的前提下执行本机 HTTP 请求，结果：

- 上传状态：`303`
- 跳转地址：`/app/library?message=上传完成：HTTP联调验收文档`

### 检索链路

执行本机 HTTP 搜索：

- 搜索 count：`3`
- Top 命中：`HTTP联调验收文档`

### 导出链路

执行本机 HTTP 导出：

- 导出状态：`HTTP/1.1 200 OK`
- 导出文件大小：`5753` bytes

## 4. 权限链路

在单独进程中设置：

```bash
OPS_ASSISTANT_REQUIRE_AUTH=true
OPS_ASSISTANT_ADMIN_USERNAME=admin
OPS_ASSISTANT_ADMIN_PASSWORD=secret123
```

验证结果：

- 未登录访问 `/app/library`：`302 -> /login?next=/app/library`
- 登录成功：`303 -> /app/library`
- 登录后访问 `/app/library`：`200`

## 5. 结论

当前项目的资料上传、索引更新、问答检索、导出、权限控制和访问日志链路已经在本机完成联调。 
